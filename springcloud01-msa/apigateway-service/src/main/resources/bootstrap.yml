# application.yml 보다 먼저 Config Server 에서 .yml 정보를 불러온다.

spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: configuration-service # 읽어오고자 하는 yml 파일 이름 (default는 application.yml)
#  profiles:
#    active: dev